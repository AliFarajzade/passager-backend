export type TTour = {
    rating: number
    ratingsAverage: number
    ratingsQuantity: number
    maxGroupSize: number
    duration: number
    price: number
    images: string[]
    name: string
    difficulty: string
    summary: string
    description: string
    coverImage: string
    createdAt: string
    startDates: string[]
}
